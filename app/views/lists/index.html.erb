<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://images.pexels.com/photos/1117132/pexels-photo-1117132.jpeg?auto=compress&cs=tinysrgb&dpr=2&h=650&w=940);">
  <div class="container">
    <h1> Save any kind of movies!</h1>
    <p>Share your favorite movie lists and receive feedback.</p>
  </div>
</div>
<div class="container">
    <h1>My movies lists:</h1>
    <%= link_to "Create a new list", new_list_path, class: "btn btn-ghost" %>
    <div class="cards">
    <% @lists.each do |list| %>
      <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path list.photo.key, height: 300, crop: :fill %>')"><%= link_to list.name, list %></div>
    <% end %>
    </div>
</div>
