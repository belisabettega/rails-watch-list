<% movies = [] %>
<% Movie.all.each do |movie|%>
    <% movies << movie.title %>
<% end%>

<%= simple_form_for [@list, Bookmark.new] do |f| %>
  <%= f.input :comment %>
  <%= f.input :movie, as: :select, collection: movies %>
  <%= f.submit %>
<% end %>